import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'standalone',
  outputFileTracingRoot: '/workspaces/exam_center/frontend',
};

export default nextConfig;
