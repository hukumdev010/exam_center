{
  "name": "exam_center_devcontainer",
  "dockerFile": "Dockerfile",
  "forwardPorts": [3000],
  "postCreateCommand": "npm install",
  "mounts": [
    "source=/home/bhabana_basnet/.ssh,target=/home/node/.ssh,type=bind,consistency=cached"
  ],
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
      }
    }
  },
  "remoteUser": "node"
}
